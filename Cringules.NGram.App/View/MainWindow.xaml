<Window x:Class="Cringules.NGram.App.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cringules.NGram.App.View"
        xmlns:vm="clr-namespace:Cringules.NGram.App.ViewModel"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <oxy:PlotView Model="{Binding Model}" Height="500" />
            <Button Command="{Binding UpdatePointsCommand}">Poof</Button>
            <Button Command="{Binding OpenPlotCommand}">Open</Button>
            <DataGrid ItemsSource="{Binding Peaks}" VerticalAlignment="Stretch"></DataGrid>
        </StackPanel>
    </Grid>
</Window>
